version: 2.1

executors:
  rust-docker:
    docker:
      - image: cimg/rust:1.82.0
    working_directory: ~/circleci-executors

jobs:
  build:
    executor: rust-docker
    steps:
      - checkout
      - run: cargo --version

workflows:
  build:
    jobs:
      - build
