version: 2.1

orbs:
  vault-orb: commercetools/vault-orb@0.7.0

executors:
  rust-docker:
    docker:
      - image: cimg/rust:1.82.0

jobs:
  build:
    executor: rust-docker
    steps:
      - checkout
      - run: cargo --version

workflows:
  build:
    jobs:
      - build
